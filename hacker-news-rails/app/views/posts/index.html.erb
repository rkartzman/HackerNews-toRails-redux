<div class="container">
	<% if current_user %>
		<p>Hello There "<span id="grandma_says"><%= @grandma %></span>"</p>
	<% end %>
	<div id="allPosts" class="p1 bg-yellow">
		<ol id="posts">
			<% @posts.each do |post| %>
			<li><a href="#" class="upvote-btn">∆</a> <%= link_to post.title, {action: "show", :id => post.id} %>  (<%= post.url %>)</li>
			<% end %>

		</ol>
	</div>

	<div>
		<%= link_to "Create New Post", {action: "new"} %>

	</div>

</div>